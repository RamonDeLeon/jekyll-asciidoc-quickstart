{
  "name": "imagekit-zod-validation",
  "version": "1.0.0",
  "description": "ImageKit upload response and webhook validation using Zod v4",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
